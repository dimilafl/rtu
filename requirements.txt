# Core dependencies
numpy>=1.20.0
pyyaml>=5.4.0

# Optional dependencies
# Install with: pip install -r requirements.txt -r requirements-optional.txt

# For development, install with: pip install -e ".[dev]"
# For plotting, install with: pip install -e ".[plot]"
